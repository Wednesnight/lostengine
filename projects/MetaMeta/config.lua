local screenWidth = 320
local screenHeight = 480

return {
  tasklet = {
    name = "Sphere",
    waitForEvents = false
  },
  window = {
    title = "Sphere",
    x = 0,
    y = 0,
    width = screenWidth,
    height = screenHeight
  },
  screenWidth = screenWidth,
  screenHeight = screenHeight
}