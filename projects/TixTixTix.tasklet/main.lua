require("MainController")

using "lost.application.WindowParams"
using "lost.math.Rect"

windowParams = WindowParams("TixTixTix", Rect(50,50,640,480))

local tixtixtix =
{
  mainController = MainController()
}
